apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: federatorai-front-end-data.pvc
  namespace: {{.NameSpace}}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi